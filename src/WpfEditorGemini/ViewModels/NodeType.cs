namespace JsonConfigEditor.ViewModels
{
    // Enum to represent the deduced or manually selected type
    public enum NodeType
    {
        String,
        Number,
        Boolean,
        Object,
        Array,
        Null
    }
} 